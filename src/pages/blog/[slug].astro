---
import BaseLayout from '../../layouts/BaseLayout.astro';
import BlogDetail from '../../components/react/BlogDetail.tsx';

export async function getStaticPaths() {
  return [{ params: { slug: '_' } }];
}

const seoTitle = 'Blog UMKM Paroki St. Servatius';
const seoDesc = 'Artikel dari Blog UMKM Paroki St. Servatius, Kampung Sawah.';
---

<BaseLayout title={seoTitle} description={seoDesc}>
  <BlogDetail client:load />
</BaseLayout>
