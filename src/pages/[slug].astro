---
import BaseLayout from '../layouts/BaseLayout.astro';
import BusinessDetail from '../components/react/BusinessDetail.tsx';

// ── Single shell page for CSR — CF Function injects real SEO ──
export async function getStaticPaths() {
  return [{ params: { slug: '_' } }];
}

const seoTitle = 'UMKM Paroki St. Servatius';
const seoDesc = 'Detail usaha UMKM Paroki St. Servatius, Kampung Sawah.';
---

<BaseLayout title={seoTitle} description={seoDesc}>
  <BusinessDetail client:load />
</BaseLayout>
